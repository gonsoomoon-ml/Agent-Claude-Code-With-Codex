# ④ Web UI — v1.1g 미디어 선택 카드 그리드 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** `SourcePicker`(미디어 선택)를 카테고리별 체크박스 리스트 → **반응형 카드 그리드**(Scope B)로. label-wrapped sr-only checkbox 로 기존 테스트·a11y 전부 보존, 코랄 선택 상태, 데이터 주도 확장.

**Architecture:** 카테고리 섹션(`<fieldset><legend>`)마다 `repeat(auto-fill, minmax(150px,1fr))` 그리드; 각 소스 = `<label className="src-card">`가 시각적으로 숨긴 native checkbox(`aria-label`, `disabled`, `toggleSource`)를 감싸고, 파생 아바타+이름+lang 배지+코랄 ✓를 보임. v1.1f `theme.ts` 코랄 토큰 재사용. hover/focus 는 index.html 전역 style.

**Tech Stack:** React + TS + Vite · vitest + @testing-library + jest-dom. deploy_web(us-east-1).

## Global Constraints
- **프론트엔드 전용**. `toggleSource`(web/src/lib/selection.ts)가 **유일한 선택 권위** — 컴포넌트에서 재구현 금지(dim 은 시각뿐). 데이터흐름·라우팅 무변경.
- **테스트/a11y 보존(비협상)**: 각 카드 = `<label>`이 sr-only **clip**(`display:none`/`visibility:hidden` 금지) native `<input type="checkbox" aria-label={s.name} checked disabled={!checked && atLimit} onChange={()=>onChange(toggleSource(selected,s.key,max))}>`를 감쌈 → `getByLabelText(name)`·`getByRole('checkbox')`·`.toBeChecked/.toBeDisabled`·MAX-5·키보드 그대로.
- **카운터 byte-identical**: `선택 {selected.length} / {max} (최대 {max}개)` (정규식 `/선택\s*1\s*\/\s*5/` 매치). sticky + cap(atLimit) 시 코랄.
- **코랄은 선택 상태에만** — v1.1f `colors`(coralFrom #FF9E80·coralTo #FF6B47·coralInk #1A0F0A) + 신규 `coralWash` 재사용. 미선택 완전 중립. 한국어 카피, 영어 식별자.

---

### Task 1: theme.ts 토큰 + SourcePicker 카드 그리드 재작성 + index.html (TDD)

**Files:**
- Modify: `web/src/theme.ts` (`colors`에 `coralWash`)
- Modify: `web/src/components/SourcePicker.tsx` (전면 재작성)
- Modify: `web/index.html` (전역 `<style>`에 `.src-card` 3줄)
- Test: `web/src/components/SourcePicker.test.tsx` (신규 테스트 추가; 기존 2개 유지)

**Interfaces:**
- Consumes: `toggleSource(selected, key, max)`(토글·MAX 초과 거부·해제 허용) · `Category{name,sources:[{key,name,lang}]}` · `colors`(theme.ts).
- Produces: `SourcePicker` 동일 props(`categories,max,selected,onChange`). 카드 = `label.src-card` + `div.src-grid`.

- [ ] **Step 1: 실패 테스트 추가** — `web/src/components/SourcePicker.test.tsx` 의 `describe` 안에 추가(기존 2 테스트는 그대로 둠):

```tsx
  it('renders each source as a card (label.src-card) reflecting selection', () => {
    render(<SourcePicker categories={cats} max={5} selected={['a']} onChange={() => {}} />)
    const aCard = screen.getByLabelText('A').closest('label')
    expect(aCard).toHaveClass('src-card')                 // 카드 구조
    expect(screen.getByLabelText('A')).toBeChecked()      // 선택 반영
    expect(screen.getByLabelText('B')).not.toBeChecked()
    expect(document.querySelector('.src-grid')).toBeTruthy()  // 그리드 컨테이너
  })
```

- [ ] **Step 2: RED** — `cd web && npx vitest run src/components/SourcePicker.test.tsx`.
  Expected: 신규 FAIL(아직 `.src-card`/`.src-grid` 없음 — `closest('label')` 가 클래스 없음/`querySelector('.src-grid')` null). 기존 2개는 PASS.

- [ ] **Step 3: `theme.ts` 토큰 추가** — `web/src/theme.ts` 의 `colors` 객체에 `coralWash` 추가:

```ts
export const colors = {
  coralFrom: '#FF9E80', coralTo: '#FF6B47', coralInk: '#1A0F0A',
  ghostBorder: 'rgba(0,0,0,0.15)', ghostText: '#444',
  coralWash: 'rgba(255,107,71,0.06)',
}
```

- [ ] **Step 4: `SourcePicker.tsx` 전면 재작성** — `web/src/components/SourcePicker.tsx` 전체를 교체:

```tsx
// web/src/components/SourcePicker.tsx — 분야별 반응형 카드 그리드 + MAX 카운터. v1.1g.
import type { CSSProperties } from 'react'
import type { Category } from '../types'
import { toggleSource } from '../lib/selection'
import { colors } from '../theme'

const SR_ONLY: CSSProperties = {
  position: 'absolute', width: 1, height: 1, padding: 0, margin: -1,
  overflow: 'hidden', clip: 'rect(0 0 0 0)', whiteSpace: 'nowrap', border: 0,
}
const CORAL_GRADIENT = `linear-gradient(135deg, ${colors.coralFrom}, ${colors.coralTo})`

interface Props {
  categories: Category[]
  max: number
  selected: string[]
  onChange: (next: string[]) => void
}

export function SourcePicker({ categories, max, selected, onChange }: Props) {
  const atLimit = selected.length >= max
  return (
    <div>
      <div style={{
        position: 'sticky', top: 0, background: '#fff', zIndex: 1,
        fontSize: 13, color: atLimit ? colors.coralTo : '#555',
        fontWeight: atLimit ? 600 : 400, padding: '4px 0', marginBottom: 8,
      }}>
        선택 {selected.length} / {max} (최대 {max}개)
      </div>
      {categories.map((cat) => (
        <fieldset key={cat.name} style={{ border: 'none', padding: 0, margin: '0 0 16px' }}>
          <legend style={{ fontWeight: 600, fontSize: 14, marginBottom: 8, padding: 0 }}>{cat.name}</legend>
          <div className="src-grid" style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(150px, 1fr))', gap: 10 }}>
            {cat.sources.map((s) => {
              const checked = selected.includes(s.key)
              const dimmed = atLimit && !checked
              const initial = s.name.trim().charAt(0)
              return (
                <label key={s.key} className="src-card" style={cardStyle(checked, dimmed)}>
                  <input
                    type="checkbox"
                    aria-label={s.name}
                    checked={checked}
                    disabled={dimmed}
                    onChange={() => onChange(toggleSource(selected, s.key, max))}
                    style={SR_ONLY}
                  />
                  <span aria-hidden="true" style={avatarStyle(checked)}>{initial}</span>
                  <span style={{ display: 'flex', flexDirection: 'column', minWidth: 0, gap: 2 }}>
                    <span style={{ fontSize: 14, fontWeight: 600, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{s.name}</span>
                    <span style={{ fontSize: 10, fontWeight: 700, letterSpacing: 0.4, color: '#999' }}>{s.lang.toUpperCase()}</span>
                  </span>
                  {checked && (
                    <span aria-hidden="true" style={{
                      position: 'absolute', top: 6, right: 6, width: 18, height: 18,
                      borderRadius: 9999, background: CORAL_GRADIENT, color: colors.coralInk,
                      fontSize: 11, fontWeight: 700, display: 'flex', alignItems: 'center', justifyContent: 'center',
                    }}>✓</span>
                  )}
                </label>
              )
            })}
          </div>
        </fieldset>
      ))}
    </div>
  )
}

function cardStyle(checked: boolean, dimmed: boolean): CSSProperties {
  return {
    position: 'relative', display: 'flex', alignItems: 'center', gap: 10,
    padding: '12px 14px', borderRadius: 12, cursor: dimmed ? 'not-allowed' : 'pointer',
    background: checked ? colors.coralWash : '#fff',
    border: checked ? `1.5px solid ${colors.coralTo}` : '1px solid #eee',
    boxShadow: checked ? '0 1px 3px rgba(255,107,71,0.18)' : 'none',
    opacity: dimmed ? 0.45 : 1,
  }
}

function avatarStyle(checked: boolean): CSSProperties {
  return {
    flexShrink: 0, width: 32, height: 32, borderRadius: 9999,
    display: 'flex', alignItems: 'center', justifyContent: 'center',
    fontSize: 14, fontWeight: 700,
    background: checked ? CORAL_GRADIENT : '#f3f3f3',
    color: checked ? colors.coralInk : '#666',
  }
}
```

- [ ] **Step 5: GREEN** — `cd web && npx vitest run src/components/SourcePicker.test.tsx src/pages/Form.trial.test.tsx src/pages/Form.cta.test.tsx`.
  Expected: 전부 PASS — 신규 카드 테스트 + 기존 SourcePicker 2(카운터·toggle→['a','b']·MAX-5 disable) + Form 의 `getByLabelText('AI Times')` 소스선택 경로.

- [ ] **Step 6: `index.html` 전역 `<style>`에 `.src-card` 추가** — `web/index.html` 의 기존 `<style>` 블록(v1.1f `.cta-coral`/`@keyframes spin` 있는 곳) 안, `@keyframes spin` 앞에 추가:

```html
      .src-card { transition: box-shadow .12s; }
      .src-card:hover { box-shadow: 0 1px 4px rgba(0, 0, 0, 0.12); }
      .src-card:focus-within { outline: 2px solid #FF6B47; outline-offset: 2px; }
```

  (선택 카드는 인라인 코랄 boxShadow 가 specificity 로 hover 를 이김 → 코랄 그림자 유지. border/배경 전부 인라인이라 hover 는 box-shadow 로.)

- [ ] **Step 7: 전체 vitest + build** — `cd web && npm test`(전부 green) · `npm run build`(타입체크+dist).
- [ ] **Step 8: Commit**

```bash
git add web/src/theme.ts web/src/components/SourcePicker.tsx web/src/components/SourcePicker.test.tsx web/index.html
git commit -m "web: SourcePicker → responsive card grid (label-wrapped sr-only checkbox; coral selected) (TDD)"
```

---

### Task 2: 배포 + 라이브 e2e (컨트롤러)

- [ ] **Step 1: 배포** — `uv run python -m briefing.webapi.deploy_web`(S3+CloudFront 무효화).
- [ ] **Step 2: 라이브 육안**(Playwright/브라우저) — `/setup` 미디어 선택:
  - 카테고리 섹션별 **카드 그리드**(폼 폭에서 ~3칸; 창 넓히면 칸 증가, 좁히면 1-2칸).
  - 카드 클릭 → **코랄 선택**(1.5px 테두리 + 옅은 wash + 아바타 코랄 + 우상단 ✓). 재클릭 → 해제.
  - **5개 선택** → 미선택 카드 흐려짐(opacity .45) + 클릭 불가. 카운터 **코랄**.
  - hover → 카드 box-shadow lift · Tab 포커스 → 코랄 outline.
  - 긴 이름 카드 … 말줄임. 체험하기/구독 흐름(소스 선택→버튼 활성) 정상.
- [ ] **Step 3: commit**(있으면).

## 완료 게이트
- `cd web && npm test` green(신규 카드 + 기존 SourcePicker 2 + Form) · `npm run build` 성공.
- 라이브: 카드 그리드 반응형 · 코랄 선택 · MAX-5 흐림 · sticky/cap 카운터 · hover/focus.

## 미룸
- **C**(성장 내비: ~8+ 카테고리 시 sticky 점프 레일/필터칩) · 카탈로그 로고·설명 필드 → 카드 확장 · `coralGradient` 공용 상수로 coralPill 통합.
