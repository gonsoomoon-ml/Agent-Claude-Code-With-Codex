# ④ Web UI — v1.2 구독하기(Subscribe) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** 로그인한(Cognito) 사용자가 선호(출처·시각)를 저장하면 매일 ⑤ 가 픽업해 발송 — `PUT /profile`(JWT-gated) → `briefing-users`(PK=Cognito sub, recipient=Cognito email) → H4 seam 이 ⑤ 로 연결.

**Architecture:** SPA Cognito PKCE → id_token → HTTP API 네이티브 JWT authorizer(/profile 라우트만) → FastAPI 가 JWT claims(scope["aws.event"])에서 sub·email 추출 → `DynamoUserStore.update_profile_from_jwt`(IDOR-safe). 보안 코어는 DI + 검증된 JWT claims 에서만.

**Tech Stack:** Python 3.12 · boto3(apigatewayv2 JWT authorizer·dynamodb·ses) · FastAPI/Mangum · React/TS(vanilla PKCE, no amplify) · pytest · vitest.

## Global Constraints
- 언어: 문서·docstring·주석 한국어 우선; 식별자 영어.
- region us-east-1 · account 057716757052.
- **보안 불변식(절대)**: PK(user_id)=`jwt.claims.sub` · recipient=`jwt.claims.email` — **body 절대 금지**(IDOR/SES 릴레이) · **id_token만**(access token 은 aud 없어 401) · `token_use=="id"` assert · **skill_md 어떤 web 경로로도 미수신**(6필드 화이트리스트) · 6필드 server-side catalog 검증.
- **Cognito 핸드셰이크**(H3): issuer=`https://cognito-idp.us-east-1.amazonaws.com/us-east-1_ANfcEK61A` · client/aud=`29ghm34nr4m2enqa6sbeua6fgn` · hostedUI=`https://briefing-users-gonsoo-057716757052.auth.us-east-1.amazoncognito.com` · callback seed=`https://dqizh0gi9cp2q.cloudfront.net/`+`http://localhost:5173/`.
- **H4 seam**: `briefing-users` PK=user_id, 7필드(skill_md 제외). `settings.users_table`. ④ = UpdateItem(6필드+recipient).
- **OPTIONS /profile 라우트 선언 금지**(preflight → $default → CORSMiddleware).
- 검증된 사실(리서치): Mangum scope key=`"aws.event"`; HTTP API route precedence = exact `METHOD /path` > $default(공존 가능); access_token 은 authorizer 통과 못 함.

---

### Task 1: `validate_profile` + `update_profile_from_jwt` (DI, TDD)

**Files:** Create `src/briefing/webapi/profile.py` · Modify `src/briefing/shared/stores/dynamo.py`(메서드 추가) · Test `tests/test_webapi_profile.py` · `tests/test_dynamo_userstore.py`

**Interfaces:** `validate_profile(fields, *, catalog_keys, lens_keys, depths, send_hours, types=("ai-news",)) -> str|None` · `DynamoUserStore.update_profile_from_jwt(*, sub, email, fields) -> None`.

- [ ] **Step 1: 실패 테스트 — validate_profile**

```python
# tests/test_webapi_profile.py
"""webapi.profile.validate_profile — 6 선호 필드 catalog 검증(순수)."""
from __future__ import annotations

from briefing.webapi.profile import validate_profile

KW = dict(catalog_keys=("aitimes", "openai"), lens_keys=("general", "engineer"),
          depths=("title-only", "summary", "full"), send_hours=(6, 7, 8))


def test_ok():
    assert validate_profile({"sources": ["aitimes"], "send_hour": 7}, **KW) is None


def test_sources_bounds_and_subset():
    assert validate_profile({"sources": []}, **KW)
    assert validate_profile({"sources": ["aitimes"] * 6}, **KW)
    assert validate_profile({"sources": ["ghost"]}, **KW)


def test_enums():
    assert validate_profile({"sources": ["aitimes"], "depth": "x"}, **KW)
    assert validate_profile({"sources": ["aitimes"], "lens": "x"}, **KW)
    assert validate_profile({"sources": ["aitimes"], "send_hour": 9}, **KW)
    assert validate_profile({"sources": ["aitimes"], "type": "stock"}, **KW)


def test_send_hour_nonint():
    assert validate_profile({"sources": ["aitimes"], "send_hour": "abc"}, **KW)
```

- [ ] **Step 2: 실패 테스트 — update_profile_from_jwt**

```python
# tests/test_dynamo_userstore.py
"""DynamoUserStore.update_profile_from_jwt — IDOR-safe(PK=sub·recipient=email 인자에서만)."""
from __future__ import annotations

from briefing.shared.stores.dynamo import DynamoUserStore


class _FakeTable:
    def __init__(self): self.updates = []
    def update_item(self, **kw): self.updates.append(kw)


def _store():
    s = DynamoUserStore.__new__(DynamoUserStore)
    s._t = _FakeTable()
    return s


def test_keys_on_sub_recipient_on_email_ignores_body_identity():
    s = _store()
    s.update_profile_from_jwt(sub="SUB-1", email="me@x.com",
                              fields={"user_id": "EVIL", "recipient": "victim@x.com",
                                      "sources": ["aitimes"], "send_hour": 8, "type": "ai-news"})
    up = s._t.updates[0]
    assert up["Key"] == {"user_id": "SUB-1"}                      # body user_id 무시
    vals = up["ExpressionAttributeValues"]
    assert ":r" in up["ExpressionAttributeValues"] or any(v == "me@x.com" for v in vals.values())
    assert "victim@x.com" not in vals.values()                   # body recipient 무시
    assert "EVIL" not in str(up["Key"])
    # type 은 DDB 예약어 → ExpressionAttributeNames 로 우회(직접 'type' 토큰 없음)
    assert "type" not in up.get("UpdateExpression", "").replace("#", "")
    assert ["aitimes"] in vals.values() and 8 in vals.values()   # sources list, send_hour int
```

- [ ] **Step 3: RED 확인** — `uv run pytest tests/test_webapi_profile.py tests/test_dynamo_userstore.py -v` → FAIL(미구현).

- [ ] **Step 4: 구현 — profile.py**

```python
# src/briefing/webapi/profile.py
"""profile — 구독 프로필 검증(순수/DI). app.py 가 JWT claims·DynamoUserStore 로 배선.

★ 보안: PK·recipient 는 *검증된 JWT claims* 에서만(app.py). 여기선 6 선호 필드 catalog 검증만.
"""
from __future__ import annotations

from collections.abc import Sequence


def validate_profile(fields: dict, *, catalog_keys, lens_keys, depths, send_hours,
                     types: Sequence[str] = ("ai-news",)) -> str | None:
    """6 선호 필드 검증 실패 메시지 or None. recipient/user_id 는 여기서 안 봄(JWT 소유)."""
    sources = fields.get("sources") or []
    if not (1 <= len(sources) <= 5):
        return "출처를 1~5개 선택하세요."
    if any(s not in set(catalog_keys) for s in sources):
        return "알 수 없는 출처가 포함되어 있습니다."
    if fields.get("type", "ai-news") not in set(types):
        return "지원하지 않는 브리핑 종류입니다."
    if fields.get("depth", "summary") not in set(depths):
        return "알 수 없는 depth."
    if fields.get("lens", "general") not in set(lens_keys):
        return "알 수 없는 lens."
    try:
        sh = int(fields.get("send_hour", 7))
    except (TypeError, ValueError):
        return "send_hour 형식 오류."
    if sh not in set(send_hours):
        return "지원하지 않는 발송 시각."
    if not isinstance(fields.get("timezone", "Asia/Seoul"), str):
        return "timezone 형식 오류."
    return None
```

- [ ] **Step 5: 구현 — dynamo.py 메서드 추가** (`DynamoUserStore` 클래스 안, `put_user` 뒤)

```python
    def update_profile_from_jwt(self, *, sub: str, email: str, fields: dict) -> None:
        """★ IDOR-safe: PK=sub·recipient=email 은 *검증된 JWT* 에서만(인자). body 는 6 선호 필드만.

        모든 속성을 #name 으로 우회 — 'type' 은 DDB 예약어. sources→list·send_hour→int.
        body 의 user_id/recipient/email 은 절대 안 씀(시그니처상 fields 에 있어도 무시).
        """
        names = {"#r": "recipient"}
        vals = {":r": email}
        sets = ["#r = :r"]
        for i, k in enumerate(("type", "sources", "depth", "lens", "send_hour", "timezone")):
            v = fields.get(k)
            if v is None:
                continue
            nk, vk = f"#f{i}", f":v{i}"
            names[nk] = k
            vals[vk] = list(v) if k == "sources" else (int(v) if k == "send_hour" else v)
            sets.append(f"{nk} = {vk}")
        self._t.update_item(
            Key={"user_id": sub}, UpdateExpression="SET " + ", ".join(sets),
            ExpressionAttributeNames=names, ExpressionAttributeValues=vals)
```

- [ ] **Step 6: GREEN + commit** — `uv run pytest tests/test_webapi_profile.py tests/test_dynamo_userstore.py -v` → pass · `uv run ruff check ...` · full `uv run pytest -q`. Commit: `webapi: validate_profile + DynamoUserStore.update_profile_from_jwt (IDOR-safe, TDD)`.

---

### Task 2: app.py `_claims` + GET/PUT /profile + CORS (TDD)

**Files:** Modify `src/briefing/webapi/app.py` · Test `tests/test_webapi_profile_route.py`

**Interfaces:** `_claims(request) -> {sub,email}` · `GET /profile`(prefill) · `PUT /profile`(validate→update). `_profile_deps()` lazy boto3.

- [ ] **Step 1: 실패 테스트**

```python
# tests/test_webapi_profile_route.py
"""GET/PUT /profile — JWT claims(scope aws.event) 추출 + IDOR-safe write. monkeypatch deps."""
from __future__ import annotations

from fastapi.testclient import TestClient

import briefing.webapi.app as appmod
from briefing.webapi.app import app


def _client(claims):
    # HTTP API v2 JWT authorizer 이벤트 모양을 scope 에 주입
    ev = {"requestContext": {"authorizer": {"jwt": {"claims": claims}}}}
    c = TestClient(app)
    c.app_event = ev
    return c


class _Store:
    def __init__(self): self.updates = []; self._rec = None
    def get_user(self, uid): return self._rec
    def update_profile_from_jwt(self, *, sub, email, fields): self.updates.append((sub, email, fields))


def _deps():
    return {"store": _Store(), "ses": None, "sender": "s@x.com",
            "keys": ["aitimes", "openai"], "lenses": ["general"], "ensure_ses": lambda e: "active"}


def _put(monkeypatch, claims, body):
    deps = _deps()
    monkeypatch.setattr(appmod, "_profile_deps", lambda: deps)
    # scope 에 aws.event 주입: TestClient 의 transport 가 scope 를 만들므로 미들웨어/의존성에서 읽도록 헤더 우회
    ev = {"requestContext": {"authorizer": {"jwt": {"claims": claims}}}}
    monkeypatch.setattr(appmod, "_event_from_request", lambda req: ev)   # app.py 가 이 헬퍼로 event 취득
    r = TestClient(app).put("/profile", json=body)
    return r, deps


def test_put_keys_on_sub_email_ignores_body(monkeypatch):
    r, deps = _put(monkeypatch, {"sub": "SUB1", "email": "me@x.com", "token_use": "id"},
                   {"user_id": "EVIL", "recipient": "victim@x.com", "sources": ["aitimes"], "send_hour": 7})
    assert r.status_code == 200
    sub, email, fields = deps["store"].updates[0]
    assert sub == "SUB1" and email == "me@x.com"


def test_put_rejects_access_token(monkeypatch):
    r, _ = _put(monkeypatch, {"sub": "S", "email": "e@x.com", "token_use": "access"},
                {"sources": ["aitimes"]})
    assert r.status_code == 401


def test_put_validation_400(monkeypatch):
    r, _ = _put(monkeypatch, {"sub": "S", "email": "e@x.com", "token_use": "id"},
                {"sources": ["ghost"]})
    assert r.status_code == 400
```

> 참고: app.py 는 `_event_from_request(req)`(= `req.scope.get("aws.event") or {}`)로 event 를 취득 — 테스트가 이 한 점을 monkeypatch 해 HTTP API 이벤트를 주입한다(scope 직접 조작 회피).

- [ ] **Step 2: RED 확인.**

- [ ] **Step 3: 구현 — app.py** (import 추가 + 헬퍼 + 라우트 + CORS)

```python
# 상단 import 에 추가
from fastapi import HTTPException, Request
from fastapi.responses import JSONResponse
from .catalog import DEPTHS, SEND_HOURS, build_catalog
from .profile import validate_profile
```
```python
# CORSMiddleware allow_methods 교체 + allow_headers
    allow_methods=["GET", "POST", "PUT", "OPTIONS"],
    allow_headers=["*", "Authorization"],
```
```python
# 헬퍼 + 라우트(/health 뒤)
def _event_from_request(req: Request) -> dict:
    return req.scope.get("aws.event") or {}        # Mangum 0.21 — HTTP API 원이벤트


def _claims(req: Request) -> dict:
    ev = _event_from_request(req)
    try:
        c = ev["requestContext"]["authorizer"]["jwt"]["claims"]
    except (KeyError, TypeError):
        raise HTTPException(status_code=401, detail="JWT claims unavailable")
    if c.get("token_use") != "id":                 # 방어심도(authorizer drift)
        raise HTTPException(status_code=401, detail="id token required")
    sub, email = c.get("sub"), c.get("email")
    if not sub or not email:
        raise HTTPException(status_code=401, detail="sub/email claim missing")
    return {"sub": sub, "email": email}


def _profile_deps() -> dict:
    import boto3
    region = os.getenv("AWS_REGION", "us-east-1")
    from ..shared.config import load_settings
    from ..shared.stores.dynamo import user_store_from_settings
    store = user_store_from_settings(load_settings())
    ses = boto3.client("ses", region_name=region)
    cat = build_catalog()
    return {"store": store, "ses": ses, "sender": os.getenv("SES_SENDER", ""),
            "keys": [s["key"] for g in cat["categories"] for s in g["sources"]],
            "lenses": [ln["key"] for ln in cat["lenses"]]}


def _ensure_ses(ses, email: str) -> str:
    """trial 패턴 재사용 — SES 미검증이면 verify 트리거. active|verification_pending."""
    attrs = ses.get_identity_verification_attributes(Identities=[email])
    st = attrs.get("VerificationAttributes", {}).get(email, {}).get("VerificationStatus")
    if st == "Success":
        return "active"
    ses.verify_email_identity(EmailAddress=email)
    return "verification_pending"


@app.get("/profile")
def get_profile(req: Request) -> dict:
    cl = _claims(req)
    rec = _profile_deps()["store"].get_user(cl["sub"])
    return {"subscribed": rec is not None, "recipient": cl["email"], "profile": rec or {}}


@app.put("/profile")
async def put_profile(req: Request):
    cl = _claims(req)
    body = await req.json()
    d = _profile_deps()
    err = validate_profile(body, catalog_keys=d["keys"], lens_keys=d["lenses"],
                           depths=DEPTHS, send_hours=SEND_HOURS)
    if err:
        return JSONResponse(status_code=400, content={"error": err})
    d["store"].update_profile_from_jwt(sub=cl["sub"], email=cl["email"], fields=body)
    return JSONResponse(status_code=200,
                        content={"status": "subscribed", "delivery": _ensure_ses(d["ses"], cl["email"])})
```
> `catalog.py` 가 `DEPTHS`/`SEND_HOURS` 를 export 하는지 확인(이미 상수 존재) — 없으면 import 경로만 조정.

- [ ] **Step 4: GREEN + commit** — pytest(3) + full green + ruff. Commit: `webapi: GET/PUT /profile + JWT claims (_claims, id-token only) + CORS PUT (TDD)`.

---

### Task 3: deploy_api JWT authorizer + /profile 라우트 + IAM + 재배포

**Files:** Modify `src/briefing/webapi/deploy_api.py`

> 실 AWS. 무TDD — 배포 + 라이브 검증(401/200/OPTIONS).

- [ ] **Step 1: IAM — `_ensure_role` 의 BriefingTrial 정책에 statement 추가**
```python
            {"Sid": "UsersTable", "Effect": "Allow",
             "Action": ["dynamodb:GetItem", "dynamodb:UpdateItem"],
             "Resource": "arn:aws:dynamodb:*:*:table/briefing-users"},
```
- [ ] **Step 2: `_ensure_http_api` 에 authorizer + /profile 라우트 추가** (route 처리 뒤, add_permission 앞)
```python
    ISSUER = "https://cognito-idp.us-east-1.amazonaws.com/us-east-1_ANfcEK61A"
    AUD = ["29ghm34nr4m2enqa6sbeua6fgn"]
    authz = next((a for a in api.get_authorizers(ApiId=api_id)["Items"] if a["Name"] == "cognito-jwt"), None)
    authz_id = authz["AuthorizerId"] if authz else api.create_authorizer(
        ApiId=api_id, Name="cognito-jwt", AuthorizerType="JWT",
        IdentitySource=["$request.header.Authorization"],
        JwtConfiguration={"Issuer": ISSUER, "Audience": AUD})["AuthorizerId"]
    routes2 = {r["RouteKey"]: r["RouteId"] for r in api.get_routes(ApiId=api_id)["Items"]}
    for rk in ("GET /profile", "PUT /profile"):       # ★ OPTIONS /profile 는 선언 안 함(preflight→$default)
        if rk in routes2:
            api.update_route(ApiId=api_id, RouteId=routes2[rk], Target=f"integrations/{integ_id}",
                             AuthorizationType="JWT", AuthorizerId=authz_id)
        else:
            api.create_route(ApiId=api_id, RouteKey=rk, Target=f"integrations/{integ_id}",
                             AuthorizationType="JWT", AuthorizerId=authz_id)
```
- [ ] **Step 3: 재배포** — `uv run python -m briefing.webapi.deploy_api`.
- [ ] **Step 4: 라이브 검증** —
```bash
source .env
# 토큰 없음 → 401
curl -s -o /dev/null -w "no-token PUT: %{http_code}\n" -X PUT "$BRIEFING_API_URL/profile" -d '{}'
# OPTIONS preflight → 200 (+CORS, $default fallthrough)
curl -s -o /dev/null -w "OPTIONS: %{http_code}\n" -X OPTIONS "$BRIEFING_API_URL/profile" \
  -H "Origin: https://x" -H "Access-Control-Request-Method: PUT"
# 공개 라우트 무손상
curl -s -o /dev/null -w "catalog(public): %{http_code}\n" "$BRIEFING_API_URL/catalog"
aws apigatewayv2 get-authorizers --api-id "$(aws apigatewayv2 get-apis --query "Items[?Name=='briefing-webapi-http'].ApiId" --output text)" --query "Items[].Name" --output text  # cognito-jwt
```
Expected: PUT no-token=401 · OPTIONS=200 · catalog=200 · authorizer=cognito-jwt. (id_token 200 검증은 Task 6 e2e.)
- [ ] **Step 5: commit** (코드만). `webapi: deploy_api Cognito JWT authorizer + /profile routes + briefing-users IAM`.

---

### Task 4: web auth/ PKCE 모듈 (TDD)

**Files:** Create `web/src/auth/{config,pkce,login,callback,session,logout}.ts` · Test `web/src/auth/pkce.test.ts` · `web/src/auth/callback.test.ts`

**Interfaces:** `generateVerifier()/generateChallenge(v)/generateState()` · `startLogin()` · `handleCallback(): Promise<void>`(state 검증→token 교환→storeTokens) · `getIdToken()/clearTokens()` · `authedFetch(url,init)`.

- [ ] **Step 1: 실패 테스트 — pkce + callback state**
```typescript
// web/src/auth/pkce.test.ts
import { describe, it, expect } from 'vitest'
import { generateVerifier, generateChallenge, generateState } from './pkce'

describe('pkce', () => {
  it('verifier is 43-128 url-safe chars', () => {
    const v = generateVerifier()
    expect(v.length).toBeGreaterThanOrEqual(43)
    expect(/^[A-Za-z0-9\-._~]+$/.test(v)).toBe(true)
  })
  it('challenge is base64url (no padding) of sha256', async () => {
    const c = await generateChallenge('abc')
    expect(c.includes('=')).toBe(false)
    expect(/^[A-Za-z0-9\-_]+$/.test(c)).toBe(true)
  })
  it('state is random', () => { expect(generateState()).not.toBe(generateState()) })
})
```
```typescript
// web/src/auth/callback.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { handleCallback } from './callback'

beforeEach(() => { sessionStorage.clear(); vi.restoreAllMocks() })

describe('handleCallback', () => {
  it('throws on state mismatch (CSRF)', async () => {
    sessionStorage.setItem('pkce_state', 'GOOD'); sessionStorage.setItem('pkce_verifier', 'V')
    history.replaceState({}, '', '/?code=C&state=EVIL')
    await expect(handleCallback()).rejects.toThrow(/state/i)
  })
  it('no-op when no code param', async () => {
    history.replaceState({}, '', '/')
    await expect(handleCallback()).resolves.toBeUndefined()
  })
})
```
- [ ] **Step 2: RED** — `cd web && npx vitest run src/auth`.
- [ ] **Step 3: 구현** — auth 모듈(아래 핵심; config/login/session/logout 포함). `pkce.ts`(Web Crypto S256·base64url), `callback.ts`(state 검증→`/oauth2/token` 교환→storeTokens, `?code` 제거), `session.ts`(in-memory id_token·authedFetch), `config.ts`(redirectUri=`location.origin+'/'`).
```typescript
// web/src/auth/pkce.ts
function b64url(buf: ArrayBuffer): string {
  return btoa(String.fromCharCode(...new Uint8Array(buf))).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')
}
export function generateVerifier(): string {
  const a = new Uint8Array(48); crypto.getRandomValues(a)
  return b64url(a.buffer)
}
export async function generateChallenge(verifier: string): Promise<string> {
  return b64url(await crypto.subtle.digest('SHA-256', new TextEncoder().encode(verifier)))
}
export function generateState(): string {
  const a = new Uint8Array(16); crypto.getRandomValues(a)
  return b64url(a.buffer)
}
```
```typescript
// web/src/auth/config.ts
export const AUTH = {
  hostedUI: 'https://briefing-users-gonsoo-057716757052.auth.us-east-1.amazoncognito.com',
  clientId: '29ghm34nr4m2enqa6sbeua6fgn',
  scope: 'openid email',
  redirectUri: () => `${location.origin}/`,
}
```
```typescript
// web/src/auth/session.ts
import { API_BASE } from '../api'
let _idToken: string | null = null
export function storeTokens(idToken: string) { _idToken = idToken }
export function getIdToken(): string | null { return _idToken }
export function clearTokens() { _idToken = null }
export function isAuthed(): boolean { return _idToken != null }
export async function authedFetch(path: string, init: RequestInit = {}): Promise<Response> {
  const headers = { ...(init.headers || {}), Authorization: `Bearer ${_idToken ?? ''}` }
  return fetch(path.startsWith('http') ? path : `${API_BASE}${path}`, { ...init, headers })
}
```
```typescript
// web/src/auth/login.ts
import { AUTH } from './config'
import { generateVerifier, generateChallenge, generateState } from './pkce'
export async function startLogin() {
  const verifier = generateVerifier(), state = generateState()
  sessionStorage.setItem('pkce_verifier', verifier)
  sessionStorage.setItem('pkce_state', state)
  const challenge = await generateChallenge(verifier)
  const q = new URLSearchParams({
    client_id: AUTH.clientId, response_type: 'code', scope: AUTH.scope,
    redirect_uri: AUTH.redirectUri(), state, code_challenge: challenge, code_challenge_method: 'S256',
  })
  location.assign(`${AUTH.hostedUI}/oauth2/authorize?${q}`)
}
```
```typescript
// web/src/auth/callback.ts
import { AUTH } from './config'
import { storeTokens } from './session'
export async function handleCallback(): Promise<void> {
  const p = new URLSearchParams(location.search)
  const code = p.get('code'); if (!code) return
  const state = p.get('state')
  const saved = sessionStorage.getItem('pkce_state')
  const verifier = sessionStorage.getItem('pkce_verifier')
  if (!state || state !== saved) throw new Error('state mismatch (CSRF)')
  if (!verifier) throw new Error('missing PKCE verifier')
  const body = new URLSearchParams({
    grant_type: 'authorization_code', client_id: AUTH.clientId, code,
    redirect_uri: AUTH.redirectUri(), code_verifier: verifier,
  })
  const r = await fetch(`${AUTH.hostedUI}/oauth2/token`, {
    method: 'POST', headers: { 'content-type': 'application/x-www-form-urlencoded' }, body })
  if (!r.ok) throw new Error(`token ${r.status}`)
  const t = await r.json()
  storeTokens(t.id_token)
  sessionStorage.removeItem('pkce_state'); sessionStorage.removeItem('pkce_verifier')
  history.replaceState({}, '', location.pathname)   // ?code 제거
}
```
```typescript
// web/src/auth/logout.ts
import { AUTH } from './config'
import { clearTokens } from './session'
export function logout() {
  clearTokens()
  location.assign(`${AUTH.hostedUI}/logout?client_id=${AUTH.clientId}&logout_uri=${encodeURIComponent(AUTH.redirectUri())}`)
}
```
- [ ] **Step 4: GREEN + commit** — `cd web && npm test`. Commit: `web: Cognito PKCE auth modules (vanilla TS, TDD)`.

---

### Task 5: web 구독하기 — api + Form + App 콜백 (TDD)

**Files:** Modify `web/src/api.ts`(getProfile/putProfile) · `web/src/App.tsx`(콜백) · `web/src/pages/Form.tsx`(구독하기) · Test `web/src/pages/Form.subscribe.test.tsx`

- [ ] **Step 1: 실패 테스트** — Form: 미인증=로그인 버튼, 인증 후 구독하기→putProfile→상태(active/verification_pending). (auth/session 을 mock.)
```tsx
// web/src/pages/Form.subscribe.test.tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import Form from './Form'
vi.mock('../auth/session', () => ({
  isAuthed: () => true, getIdToken: () => 'tok',
  authedFetch: vi.fn(async () => ({ ok: true, json: async () => ({ status: 'subscribed', delivery: 'active' }) })),
}))
vi.mock('../auth/login', () => ({ startLogin: vi.fn() }))
beforeEach(() => {
  vi.stubGlobal('fetch', vi.fn(async (u: string) => {
    if (String(u).endsWith('/catalog')) return { ok: true, json: async () => ({
      categories: [{ name: '전체', sources: [{ key: 'aitimes', name: 'AI Times', lang: 'ko' }] }],
      lenses: [], depths: [], send_hours: [6, 7, 8], max_sources: 5 }) }
    return { ok: true, json: async () => ({ subscribed: false, recipient: 'me@x.com', profile: {} }) }
  }) as unknown as typeof fetch)
})
describe('구독하기', () => {
  it('subscribes and shows delivery status', async () => {
    render(<Form />)
    await waitFor(() => screen.getByLabelText('AI Times'))
    fireEvent.click(screen.getByLabelText('AI Times'))
    fireEvent.click(screen.getByRole('button', { name: /구독하기/ }))
    await waitFor(() => expect(screen.getByText(/구독 완료|매일/)).toBeInTheDocument())
  })
})
```
- [ ] **Step 2: RED → 구현** — `api.ts` getProfile/putProfile(authedFetch); `App.tsx` 마운트 시 `handleCallback().catch(...)`; `Form.tsx` 구독하기 버튼: 미인증→`startLogin()`, 인증→`putProfile({sources,send_hour,lens:'general',depth:'summary'})`→상태(`delivery==='active'`?"구독 완료 — 매일 발송":"구독 저장 — 메일의 SES 인증 클릭 후 발송"). 인증 시 "구독 주소: {getProfile().recipient}" 표시, 마운트 prefill.
- [ ] **Step 3: GREEN + build + commit** — `cd web && npm test && npm run build`. Commit: `web: 구독하기 — PKCE login + PUT /profile + delivery status (TDD)`.

---

### Task 6: deploy_web + e2e (Cognito 로그인 → 구독)

- [ ] **Step 1: web 재배포** — `uv run python -m briefing.webapi.deploy_web`.
- [ ] **Step 2: 보안 라이브 검증**(토큰 기반) — Task 3 의 401(no-token)/OPTIONS 200 재확인 + (가능하면) Cognito 로그인으로 받은 id_token 으로 `PUT /profile` 200 · access_token 으로 401.
- [ ] **Step 3: e2e** — 브라우저로 사이트 → 로그인(Cognito hosted UI, verified 주소 가입/로그인) → 폼 → 구독하기 → 200 `{status:subscribed, delivery}`. 확인:
```bash
source .env
aws dynamodb scan --table-name briefing-users --region us-east-1 \
  --filter-expression "attribute_exists(recipient)" --max-items 5 \
  --query "Items[].{uid:user_id.S,rec:recipient.S,sh:send_hour.N}" --output table
```
→ PK=sub 행에 recipient=가입 email·선택 필드. (⑤ 픽업은 다음 07:00 KST tick — 또는 mode=scheduled 강제 invoke 로 확인.)
- [ ] **Step 4: commit**(있으면).

---

## 완료 게이트
- pytest(profile·dynamo·route) + vitest(pkce·callback·subscribe) green. 라이브: PUT no-token 401 · OPTIONS 200 · catalog 200 · authorizer=cognito-jwt · e2e 구독 행(PK=sub·recipient).
- 보안 불변식 확인: body user_id/recipient 무시(테스트) · access_token→401 · skill_md 미수신.

## 미룸 (이 플랜 밖)
RequirePKCE(LANE-A H5) · 자가가입 DoS cap · 토큰 BFF/HttpOnly · WEB_ORIGIN 좁히기 · v1.1b(SES production).
