# ④ Web UI — v1.1e 테스트 주소 쿨다운 우회 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** `.env` allowlist(`TRIAL_TEST_EMAILS`)에 등록한 주소만 체험하기 1시간 쿨다운을 건너뛰어 자주 테스트 — 공개 사용자 가드(쿨다운+cap)는 무변경.

**Architecture:** `handle_trial` 의 쿨다운 게이트 한 줄을 `if email not in test_emails and store.within_cooldown(email)` 로 바꾸는 단일 분기. allowlist 는 `_parse_emails` 로 정규화한 frozenset(`app.py` 가 env 에서 읽어 DI 주입, `deploy_api.py` 가 Lambda env 로 pass-through). cap·SES·record·invoke 는 무변경.

**Tech Stack:** Python 3.12 · FastAPI/Mangum(Lambda) · pytest(DI fake) · boto3. region us-east-1.

## Global Constraints
- 한국어 UI 카피; 영어 식별자(함수/변수/env/CLI/경로).
- **공개 가드 무변경**: 비-allowlist 주소는 쿨다운(`within_cooldown`) + 전역 cap 그대로 적용.
- **전역 cap = 절대 천장**: `TRIAL_GLOBAL_CAP` 하드코딩 `"50"` + `deploy_api.py` "env 패스스루 금지" 주석 **유지**. cap 은 allowlist 주소도 카운트(우회 금지).
- **미설정/빈값 불변식**: `test_emails` 기본값 `frozenset()` → `TRIAL_TEST_EMAILS` 없거나 `""` → 오늘 동작과 byte-identical.
- **행 기록 유지**: `store.record(email, status, ttl=...)` 는 모든 경로에서 동일 호출(모달 status 폴링 보존). 쿨다운 *검사*만 skip, *기록*은 그대로.
- **정규화 일치**: `handle_trial` 이 `email = (...).strip().lower()`(trial.py:64) 하므로 allowlist 도 소문자·trim 정규화.

---

### Task 1: `trial.py` — `_parse_emails` + 쿨다운 우회 분기 (TDD)

**Files:**
- Modify: `src/briefing/webapi/trial.py` (handle_trial 시그니처 + trial.py:69 쿨다운 분기; `_parse_emails` 신규)
- Test: `tests/test_webapi_trial.py` (4 신규 단위 테스트)

**Interfaces:**
- Consumes: 기존 `handle_trial(payload, *, store, ses, runtime_invoke, cap, cooldown_s, today, catalog_keys)` · `_FakeStore(cooldown=, over=)` · `_FakeSes(verified=)` · `_invoke_spy()` · `_ok_payload(**o)`(email="u@x.com") · `CATALOG=("aitimes","openai","anthropic")`.
- Produces: `_parse_emails(raw: str) -> frozenset[str]` · `handle_trial(..., test_emails: frozenset[str] = frozenset())`(새 키워드, 기본 빈 set).

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_webapi_trial.py` 끝에 추가(상단 import 에 `_parse_emails` 합류: `from briefing.webapi.trial import TrialStore, handle_trial, validate_trial, _parse_emails`):

```python
# ── v1.1e: TRIAL_TEST_EMAILS allowlist 쿨다운 우회 ──────────────────────

def test_allowlist_email_skips_cooldown():
    """allowlist 주소는 within_cooldown=True 여도 202(쿨다운 게이트 skip)."""
    inv = _invoke_spy()
    code, body = handle_trial(_ok_payload(), store=_FakeStore(cooldown=True),
                              ses=_FakeSes(verified=True), runtime_invoke=inv,
                              cap=50, cooldown_s=3600, today="d", catalog_keys=CATALOG,
                              test_emails=frozenset({"u@x.com"}))
    assert code == 202 and body["status"] == "sending"
    assert inv.calls and inv.calls[0][0] == "trial"   # 우회 후 정상 invoke


def test_non_allowlist_email_still_cooldown_429():
    """allowlist 가 비어있지 않아도 그 안에 없는 주소는 429(공개 가드 유지)."""
    inv = _invoke_spy()
    code, _ = handle_trial(_ok_payload(), store=_FakeStore(cooldown=True),
                           ses=_FakeSes(), runtime_invoke=inv,
                           cap=50, cooldown_s=3600, today="d", catalog_keys=CATALOG,
                           test_emails=frozenset({"other@x.com"}))
    assert code == 429 and not inv.calls


def test_allowlist_email_still_blocked_by_cap():
    """allowlist 우회는 쿨다운 한정 — 전역 cap 은 테스트 주소도 차단(절대 천장 유지)."""
    inv = _invoke_spy()
    code, _ = handle_trial(_ok_payload(), store=_FakeStore(over=True),
                           ses=_FakeSes(verified=True), runtime_invoke=inv,
                           cap=50, cooldown_s=3600, today="d", catalog_keys=CATALOG,
                           test_emails=frozenset({"u@x.com"}))
    assert code == 429 and not inv.calls


def test_parse_emails_normalizes():
    """쉼표 구분 → 소문자·trim·빈값 제거 frozenset; '' → frozenset()."""
    assert _parse_emails("A@x.com, b@y.com ,") == frozenset({"a@x.com", "b@y.com"})
    assert _parse_emails("") == frozenset()
    assert _parse_emails("  ") == frozenset()
```

> 참고: "빈 allowlist 면 쿨다운 enforced"(미설정 불변식)는 기존 `test_handle_cooldown_and_cap_429`(test_emails 미전달=기본 frozenset → 429)가 이미 커버 → DRY 위해 중복 테스트 생략.

- [ ] **Step 2: RED** — `uv run pytest tests/test_webapi_trial.py -q`.
  Expected: 4 신규 테스트 FAIL — `ImportError: cannot import name '_parse_emails'` (또는 `handle_trial() got an unexpected keyword argument 'test_emails'`).

- [ ] **Step 3: 구현 — `_parse_emails`** — `src/briefing/webapi/trial.py` 의 `validate_trial` 위(모듈 상단 `_EMAIL_RE` 아래)에 추가:

```python
def _parse_emails(raw: str) -> frozenset[str]:
    """쉼표 구분 이메일 → 정규화(소문자·trim·빈값 제거) frozenset. '' → frozenset()."""
    return frozenset(e.strip().lower() for e in (raw or "").split(",") if e.strip())
```

- [ ] **Step 4: 구현 — handle_trial 시그니처 + 쿨다운 분기** — `handle_trial` 시그니처(trial.py:59-62)에 키워드 인자 추가:

```python
def handle_trial(
    payload: dict, *, store, ses, runtime_invoke: Callable[[str, dict], None],
    cap: int, cooldown_s: int, today: str, catalog_keys,
    test_emails: frozenset[str] = frozenset(),
) -> tuple[int, dict]:
```

  그리고 쿨다운 게이트(trial.py:69-70)를 변경:

```python
    if email not in test_emails and store.within_cooldown(email):
        return 429, {"error": "최근에 이미 요청했어요. 잠시 후 다시 시도하세요."}
```

  (그 외 — validate, `over_global_cap`, SES verify, `record`, `runtime_invoke`, 반환 — 무변경.)

- [ ] **Step 5: GREEN** — `uv run pytest tests/test_webapi_trial.py -q`.
  Expected: 전부 PASS(기존 + 신규 4). 기본 인자라 기존 호출(미전달)은 무손상.

- [ ] **Step 6: lint** — `uv run ruff check src tests`. Expected: clean.

- [ ] **Step 7: Commit**

```bash
git add src/briefing/webapi/trial.py tests/test_webapi_trial.py
git commit -m "webapi: TRIAL_TEST_EMAILS allowlist skips trial cooldown (cap unchanged) (TDD)"
```

---

### Task 2: `app.py`/`deploy_api.py` 배선 + 라우트 테스트 + 배포·e2e

**Files:**
- Modify: `src/briefing/webapi/app.py` (`_parse_emails` import · `_trial_deps` 반환 · `post_trial` 호출)
- Modify: `src/briefing/webapi/deploy_api.py` (Lambda env `TRIAL_TEST_EMAILS` pass-through)
- Test: `tests/test_webapi_trial_route.py` (`_fake_deps` 갱신 + 2 라우트 테스트)

**Interfaces:**
- Consumes: Task 1 의 `_parse_emails` · `handle_trial(..., test_emails=)`. 기존 `_trial_deps()` 반환 dict(store/ses/runtime_invoke/sender/cap/cooldown_s) · route fixture `_fake_deps()` · `monkeypatch.setattr(appmod, "_trial_deps", ...)`.
- Produces: `_trial_deps()` 반환에 `"test_emails": frozenset[str]` 키 추가; `post_trial` 가 `handle_trial` 에 `test_emails=d["test_emails"]` 전달.

- [ ] **Step 1: 실패 테스트 작성 (라우트 배선)** — 먼저 `tests/test_webapi_trial_route.py` 의 `_fake_deps()`(line 25-29) 반환 dict 에 `"test_emails": frozenset()` 한 키 추가(이게 없으면 Step 3 후 `post_trial` 의 `d["test_emails"]` 가 기존 테스트에서 KeyError):

```python
def _fake_deps():
    inv = []
    return {"store": _Store(), "ses": _Ses(),
            "runtime_invoke": lambda mode, p: inv.append((mode, p)), "sender": "s@x.com",
            "cap": 50, "cooldown_s": 3600, "test_emails": frozenset(), "_inv": inv}
```

  그다음 파일 끝에 추가:

```python
class _CooldownStore:
    """within_cooldown 항상 True — 우회 분기 검증용."""
    def within_cooldown(self, e): return True
    def over_global_cap(self, d, c): return False
    def record(self, e, s, *, ttl=0): pass


def test_post_trial_allowlist_bypasses_cooldown(monkeypatch):
    deps = _fake_deps()
    deps["store"] = _CooldownStore()
    deps["test_emails"] = frozenset({"u@x.com"})
    monkeypatch.setattr(appmod, "_trial_deps", lambda: deps)
    r = client.post("/trial", json={"email": "u@x.com", "sources": ["aitimes"]})
    assert r.status_code == 202


def test_post_trial_non_allowlist_hits_cooldown(monkeypatch):
    deps = _fake_deps()
    deps["store"] = _CooldownStore()
    deps["test_emails"] = frozenset()       # 빈 allowlist → 공개 가드
    monkeypatch.setattr(appmod, "_trial_deps", lambda: deps)
    r = client.post("/trial", json={"email": "u@x.com", "sources": ["aitimes"]})
    assert r.status_code == 429
```

- [ ] **Step 2: RED** — `uv run pytest tests/test_webapi_trial_route.py -q`.
  Expected: 신규 2 FAIL — allowlist 테스트가 429 반환(아직 `post_trial` 이 test_emails 를 전달 안 함). (만약 `_fake_deps` 에 test_emails 를 아직 안 넣었다면 기존 테스트가 KeyError 로 먼저 깨짐 — Step 1 에서 `_fake_deps` 에 `"test_emails": frozenset()` 추가했는지 확인.)

- [ ] **Step 3: 구현 — `app.py`** — (a) 상단 trial import 에 `_parse_emails` 합류(예: `from .trial import TrialStore, handle_trial, _parse_emails` — 기존 import 라인에 이름 추가). (b) `_trial_deps()` 반환 dict(app.py:151-153)에 키 추가:

```python
    return {"store": TrialStore(table), "ses": ses, "runtime_invoke": runtime_invoke,
            "sender": os.getenv("SES_SENDER", ""), "cap": int(os.getenv("TRIAL_GLOBAL_CAP", "50")),
            "cooldown_s": int(os.getenv("TRIAL_COOLDOWN_SECONDS", "3600")),
            "test_emails": _parse_emails(os.getenv("TRIAL_TEST_EMAILS", ""))}
```

  (c) `post_trial` 의 `handle_trial(...)` 호출(app.py:164-167)에 인자 추가:

```python
    code, body = handle_trial(
        payload, store=d["store"], ses=d["ses"], runtime_invoke=d["runtime_invoke"],
        cap=d["cap"], cooldown_s=d["cooldown_s"],
        today=now.strftime("%Y-%m-%d"), catalog_keys=keys,
        test_emails=d["test_emails"])
```

- [ ] **Step 4: GREEN** — `uv run pytest tests/test_webapi_trial_route.py tests/test_webapi_trial.py -q`.
  Expected: 전부 PASS(기존 route 3 + 신규 2 + Task1 전체).

- [ ] **Step 5: 구현 — `deploy_api.py` env pass-through** — `_deploy_lambda` 의 `env["Variables"]`(deploy_api.py:110-116)에 한 줄 추가(`TRIAL_GLOBAL_CAP "50"` 과 그 주석은 **그대로 유지**):

```python
    env = {"Variables": {
        "WEB_ORIGIN": "*",
        "BRIEFING_RUNTIME_ARN": os.getenv("BRIEFING_RUNTIME_ARN", ""),
        "BRIEFING_TRIALS_TABLE": "briefing-trials",
        "SES_SENDER": settings.ses_sender,
        "TRIAL_GLOBAL_CAP": "50",   # hard cap — ⑤ 보호. 올리려면 코드 변경(고의적). env 패스스루 금지.
        "TRIAL_COOLDOWN_SECONDS": "3600",
        "TRIAL_TEST_EMAILS": os.getenv("TRIAL_TEST_EMAILS", ""),   # v1.1e: owner 테스트 주소 쿨다운 우회(빈=무변경)
    }}
```

- [ ] **Step 6: 전체 회귀 + lint** — `uv run pytest -q` (전 스위트) · `uv run ruff check src tests`. Expected: 전부 PASS·clean.

- [ ] **Step 7: Commit**

```bash
git add src/briefing/webapi/app.py src/briefing/webapi/deploy_api.py tests/test_webapi_trial_route.py
git commit -m "webapi: wire TRIAL_TEST_EMAILS through _trial_deps + Lambda env pass-through"
```

- [ ] **Step 8: 배포 + 실 e2e** (사람/컨트롤러 단계 — 단위테스트 아님):
  1. `.env` 에 `TRIAL_TEST_EMAILS=moongons@amazon.com,gonsoomoon@gmail.com` 추가(gitignored — 커밋 금지).
  2. `uv run python -m briefing.webapi.deploy_api` (Lambda env 갱신).
  3. 라이브 e2e: `/setup` 에서 moongons 로 **체험하기 연속 2회** → **둘 다 202**(429 없음), 모달 `생성 중→발송`. 대조: 비-allowlist 주소는 2회차 429 유지.
  4. (배포 검증) `aws lambda get-function-configuration --function-name <name> --query 'Environment.Variables.TRIAL_TEST_EMAILS'` → 설정값 확인.

## 완료 게이트
- `uv run pytest -q` 전부 green(Task1 4 + Task2 2 신규 포함) · `ruff` clean.
- 라이브: allowlist 주소 연속 체험 2회 모두 202, 비-allowlist 는 2회차 429.
- 불변식 확인: `TRIAL_TEST_EMAILS` 미설정 시 기존 동작 무변경(기본 frozenset).

## 미룸
- per-test-email 별 cap 분리 · allowlist UI 관리 · 우회 시 감사 로그 · cap 우회(절대 천장 제거 — 의도적 비채택).
