# ④ Web UI — v1.1f CTA 시그니처 코랄 pill Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** 레퍼런스의 시그니처 코랄 pill CTA를 적용 — 체험하기를 1번째 primary 코랄 pill로 재배치/재스타일, 옆 버튼은 ghost, 랜딩 CTA도 코랄 pill, + v1.1d 미룬 `@keyframes spin`(⏳ 회전) 동봉.

**Architecture:** 새 `web/src/theme.ts`(coralPill/ghostPill/coralDisabled 토큰)를 Form/Landing이 인라인 스프레드로 소비; hover/disabled-glyph/스피너 키프레임은 `web/index.html`의 전역 `<style>`. 라이트 레이아웃·핸들러·데이터흐름 무변경.

**Tech Stack:** React + TS + Vite · vitest + @testing-library + jest-dom. region us-east-1(deploy_web).

## Global Constraints
- **프론트엔드 전용**(`web/`). 백엔드·라우팅·데이터흐름·검증 로직 무변경.
- **핸들러 verbatim**: `handleTrial`/`handleSubscribe`/`startLogin` + 체험 disabled 게이트(`isTrialSubmitting || !(selected.length && /^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email))`) 동작 무변경(리팩터=상수 호이스트만).
- **접근성 이름 유지**: `▶`/`→` 는 `<span aria-hidden="true">` → 버튼 접근이름 `/체험하기/`·`/구독하기/` 그대로(기존 vitest regex 매처 green).
- **색/형태 정확값**: coralFrom `#FF9E80` · coralTo `#FF6B47` · coralInk `#1A0F0A` · gradient `linear-gradient(135deg, … 0%, … 100%)` · borderRadius `9999`.
- **라이트 캔버스·정보구조·샘플 iframe 무변경**(신뢰 브랜드). 한국어 카피, 영어 식별자.

---

### Task 1: `theme.ts` + /setup CTA 코랄 pill 재배치/재스타일 + index.html (TDD)

**Files:**
- Create: `web/src/theme.ts`
- Modify: `web/src/pages/Form.tsx` (액션행 L176-203 재배치/재스타일 + `trialDisabled` 호이스트)
- Modify: `web/index.html` (전역 `<style>`: hover/disabled + `@keyframes spin`)
- Test: `web/src/pages/Form.cta.test.tsx` (신규)

**Interfaces:**
- Consumes: `Form` 기존 state(`authed`,`isTrialSubmitting`,`isSubSubmitting`,`selected`,`email`,`submitting`) + 핸들러. 테스트 셋업 패턴 = `Form.trial.test.tsx`(fetch stub `/catalog`).
- Produces: `web/src/theme.ts` exports `coralPill: CSSProperties`·`ghostPill: CSSProperties`·`coralDisabled: CSSProperties`·`colors`. 버튼 className `cta-coral`(체험하기/랜딩)·`cta-ghost`(사이드).

- [ ] **Step 1: 실패 테스트 작성** — `web/src/pages/Form.cta.test.tsx`:

```tsx
// Form.cta.test.tsx — v1.1f: 체험하기 primary 코랄 pill(순서·클래스·게이트 보존)
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import Form from './Form'

beforeEach(() => {
  vi.stubGlobal('fetch', vi.fn(async (url: string) => {
    if (String(url).endsWith('/catalog')) return {
      ok: true, json: async () => ({
        categories: [{ name: '전체', sources: [{ key: 'aitimes', name: 'AI Times', lang: 'ko' }] }],
        lenses: [], depths: [], send_hours: [6, 7, 8], max_sources: 5,
      }),
    }
    return { ok: true, status: 202, json: async () => ({ status: 'verification_pending' }) }
  }) as unknown as typeof fetch)
})
afterEach(() => vi.restoreAllMocks())

describe('v1.1f CTA 코랄 pill', () => {
  it('체험하기 = primary coral pill (first, cta-coral); sibling = ghost; disabled gate preserved', async () => {
    render(<Form />)
    await waitFor(() => screen.getByLabelText('AI Times'))

    // 액션행 버튼은 정확히 2개(<button>); 소스=checkbox·시각=radio 는 제외
    const buttons = screen.getAllByRole('button')
    expect(buttons).toHaveLength(2)
    expect(buttons[0]).toHaveClass('cta-coral')        // 체험하기 primary, 첫 자식
    expect(buttons[0].textContent).toMatch(/체험하기/)
    expect(buttons[1]).toHaveClass('cta-ghost')        // 로그인/구독하기 강등
    expect(buttons[1].textContent).toMatch(/구독하기/)

    // disabled 게이트 보존: 출처/이메일 전엔 disabled
    expect(buttons[0]).toBeDisabled()
    fireEvent.click(screen.getByLabelText('AI Times'))
    fireEvent.change(screen.getByPlaceholderText('you@example.com'), { target: { value: 'u@x.com' } })
    expect(screen.getByRole('button', { name: /체험하기/ })).not.toBeDisabled()
  })
})
```

- [ ] **Step 2: RED** — `cd web && npx vitest run src/pages/Form.cta.test.tsx`.
  Expected: FAIL — `buttons[0]` 이 (아직 재배치 전) 로그인/구독하기라 `toHaveClass('cta-coral')` 실패(또는 import 에러 없음 — theme 은 Form 이 아직 안 씀).

- [ ] **Step 3: `theme.ts` 생성** — `web/src/theme.ts`:

```ts
// web/src/theme.ts — v1.1f: 시그니처 코랄 pill CTA 토큰(레퍼런스 차용).
// base look 은 인라인 스프레드, hover/disabled-glyph 는 index.html 의 전역 <style>.
import type { CSSProperties } from 'react'

export const colors = {
  coralFrom: '#FF9E80', coralTo: '#FF6B47', coralInk: '#1A0F0A',
  ghostBorder: 'rgba(0,0,0,0.15)', ghostText: '#444',
}

export const coralPill: CSSProperties = {
  display: 'inline-flex', alignItems: 'center', gap: 8,
  borderRadius: 9999, padding: '14px 28px', fontSize: 16, fontWeight: 600,
  color: colors.coralInk,
  background: `linear-gradient(135deg, ${colors.coralFrom} 0%, ${colors.coralTo} 100%)`,
  border: '1px solid transparent', cursor: 'pointer', textDecoration: 'none',
  boxShadow: '0 1px 2px rgba(0,0,0,0.08)',
}

export const ghostPill: CSSProperties = {
  display: 'inline-flex', alignItems: 'center', gap: 8,
  borderRadius: 9999, padding: '12px 22px', fontSize: 14, fontWeight: 600,
  color: colors.ghostText, background: 'transparent',
  border: `1px solid ${colors.ghostBorder}`, cursor: 'pointer', textDecoration: 'none',
}

export const coralDisabled: CSSProperties = {
  background: '#ececec', color: '#aaa', boxShadow: 'none', cursor: 'not-allowed',
}
```

- [ ] **Step 4: `Form.tsx` 재배치/재스타일** — (a) import 추가(상단 import 블록): `import { coralPill, ghostPill, coralDisabled } from '../theme'`. (b) `const isSubSubmitting = submitting === 'subscribe'`(현 L136) 바로 아래에 호이스트:

```tsx
  const trialDisabled = isTrialSubmitting || !(selected.length && /^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email))
```

  (c) 액션행 div(현 L176-203) 전체를 아래로 교체(체험하기 first=coral, 사이드 second=ghost):

```tsx
      <div style={{ marginTop: 24, display: 'flex', gap: 12, alignItems: 'center', flexWrap: 'wrap' }}>
        <button
          type="button"
          className="cta-coral"
          disabled={trialDisabled}
          onClick={handleTrial}
          style={{ ...coralPill, ...(trialDisabled ? coralDisabled : null) }}
        >
          {isTrialSubmitting ? '보내는 중…' : <><span aria-hidden="true">▶</span> 체험하기</>}
        </button>
        {authed ? (
          <button
            type="button"
            className="cta-ghost"
            disabled={isSubSubmitting || !selected.length}
            onClick={handleSubscribe}
            style={ghostPill}
          >
            {isSubSubmitting ? '보내는 중…' : <>구독하기 <span aria-hidden="true">→</span></>}
          </button>
        ) : (
          <button
            type="button"
            className="cta-ghost"
            onClick={() => startLogin()}
            style={ghostPill}
          >
            로그인 / 구독하기 <span aria-hidden="true">→</span>
          </button>
        )}
      </div>
```

- [ ] **Step 5: GREEN** — `cd web && npx vitest run src/pages/Form.cta.test.tsx src/pages/Form.trial.test.tsx src/pages/Form.progress.test.tsx src/pages/Form.subscribe.test.tsx`.
  Expected: 전부 PASS(신규 + 기존 4 — `aria-hidden` 덕에 `/체험하기/`·`/구독하기/`·`/보내는 중/` 매처 무손상).

- [ ] **Step 6: `index.html` 전역 `<style>`** — `web/index.html` `<head>`의 `<title>` 다음 줄에 추가(테스트 없음 — 정적 CSS, e2e 육안 검증):

```html
    <style>
      .cta-coral { transition: all .15s; }
      .cta-coral:hover:not([disabled]) {
        filter: brightness(1.08);
        box-shadow: 0 0 24px rgba(255, 107, 71, 0.35);
        transform: translateY(-1px);
      }
      .cta-coral[disabled] > [aria-hidden] { display: none; }
      .cta-ghost:hover {
        border-color: rgba(0, 0, 0, 0.45);
        color: #111;
        background: rgba(0, 0, 0, 0.03);
      }
      @keyframes spin { to { transform: rotate(360deg); } }
    </style>
```

- [ ] **Step 7: 전체 vitest + build** — `cd web && npm test`(전부 green) · `npm run build`(타입체크+dist).
- [ ] **Step 8: Commit**

```bash
git add web/src/theme.ts web/src/pages/Form.tsx web/src/pages/Form.cta.test.tsx web/index.html
git commit -m "web: 체험하기 primary coral pill + ghost sibling + index.html hover/@keyframes spin (TDD)"
```

---

### Task 2: 랜딩 CTA 코랄 pill + 배포 + e2e

**Files:**
- Modify: `web/src/pages/Landing.tsx` (구독 설정하기 Link L16-18 → coral pill)

**Interfaces:**
- Consumes: Task 1 의 `coralPill`(theme.ts) + `cta-coral` 전역 hover(index.html).

- [ ] **Step 1: `Landing.tsx` CTA 교체** — import 추가 `import { coralPill } from '../theme'`, 그리고 `<Link>`(L16-18)을 교체:

```tsx
      <div style={{ marginTop: 20 }}>
        <Link to="/setup" className="cta-coral" style={coralPill}>
          구독 설정하기 <span aria-hidden="true">→</span>
        </Link>
      </div>
```

  (네비게이션이라 `▶` 아님 · trailing `→` 유지 · 구조 무변경. Landing 단위테스트 없음 — 빌드 타입체크로 충분.)

- [ ] **Step 2: 전체 vitest + build** — `cd web && npm test`(green, 회귀 없음) · `npm run build`.

- [ ] **Step 3: Commit**

```bash
git add web/src/pages/Landing.tsx
git commit -m "web: landing 구독 설정하기 CTA → coral pill (→)"
```

- [ ] **Step 4: 배포 + 라이브 e2e** (컨트롤러 단계 — 단위테스트 아님):
  1. `uv run python -m briefing.webapi.deploy_web`(S3+CloudFront 재배포, 무효화 포함).
  2. 라이브 육안(Playwright/브라우저) — `/setup`: 체험하기=코랄 pill(▶, **primary 첫 자리**)·옆=ghost(테두리, →) · hover 글로우/부상 · 출처/이메일 전엔 회색 disabled(▶ 숨김). `/`(랜딩): 구독 설정하기=코랄 pill(→). **체험하기 클릭 → ProgressModal ⏳ 실제 회전**(키프레임).

## 완료 게이트
- `cd web && npm test` 전부 green(신규 Form.cta + 기존 Form/ProgressModal/SourcePicker) · `npm run build` 성공.
- 라이브: /setup 체험하기 코랄 primary pill + 옆 ghost · 랜딩 CTA 코랄 · 모달 ⏳ 회전 · disabled 회색.

## 미룸
- B(다크 히어로 하이브리드) · C(풀 에디토리얼) · 폰트 CDN · 다크 대비 a11y 스윕. (`theme.ts` 가 B 진입점.)
