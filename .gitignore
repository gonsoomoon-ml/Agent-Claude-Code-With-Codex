# Claude Code machine-local settings (personal overrides) — not committed
.claude/settings.local.json

# Python / UV
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Local env & data
.env
.data/

# ② AgentCore 배포 빌드 산출물 (deploy_runtime.py staging + toolkit 생성물)
.agentcore_build/
.bedrock_agentcore.yaml
.bedrock_agentcore/

# users/<id>/ = PII (실제 recipient 이메일·프로필). gonsoo 예시(개발자 본인)만 커밋;
# 실제 사용자 디렉토리는 무시(웹-UI 다중 사용자는 파일 아닌 DB, v2).
/users/*
!/users/gonsoo/
!/users/README.md
!/users/445814b8-5001-70a6-84a6-6c010ac347ba/
.gw_build/
.gw_build.zip

# Playwright MCP 임시 스냅샷/스크린샷(테스트 아티팩트 — 커밋 금지)
.playwright-mcp/
*.jpeg

# PPTX 슬라이드 — 바이너리 산출물(미커밋). 재생성: docs/deck/make_slide.py
*.pptx

# superpowers 작업 스크래치(SDD 레저·브레인스토밍 목업) — 미커밋
.superpowers/
